@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1>Scegli il DMBS verso cui aprire la connessione</h1>
    <a asp-page="AddSQLite" class="btn btn-primary">SQLite</a>
    <a asp-page="AddSQLServer" class="btn btn-primary">SQLServer</a>
    <a asp-page="AddSQLServerLocal" class="btn btn-primary">SQLServer Locale</a>
</div>
